# custom_checks.conf

# Define custom checks
[CustomChecks]
# Check for specific users
check_users = user1,user2

# Check for specific services
check_services = apache2,nginx

# Check for specific ports
check_ports = 80,443

# Custom commands
custom_command_1 = ls -l /etc/passwd
custom_command_2 = netstat -tuln
